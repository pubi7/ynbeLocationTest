# Утаснаас Backend шууд хандах заавар

## ✅ Тийм, утаснаас backend-д шууд хандах боломжтой!

## Шалгах арга

### 1. Утасны browser дээр шалгах

Утасны browser (Chrome, Safari гэх мэт) нээж, доорх URL-уудыг оролдох:

#### Backend сервер ажиллаж байгаа эсэхийг шалгах:
```
http://192.168.1.6:3000
```

#### Products API шалгах:
```
http://192.168.1.6:3000/api/products
```

#### Login API шалгах (POST request):
- Browser дээр шууд POST хийх боломжгүй, гэхдээ app дээр ажиллана

### 2. App дээр шалгах

1. **Settings** руу орох
2. **Server URL** талбарт: `http://192.168.1.6:3000` байгаа эсэхийг шалгах
3. **Connect** дарах
4. Холбогдсон эсэхийг шалгах

## Боломжтой API Endpoints

### Authentication
- `POST http://192.168.1.6:3000/api/auth/login`
  - Body: `{ "identifier": "admin@admin.com", "password": "password" }`

### Products
- `GET http://192.168.1.6:3000/api/products` - Бүх бүтээгдэхүүн авах
- `POST http://192.168.1.6:3000/api/products` - Шинэ бүтээгдэхүүн нэмэх

### Orders
- `GET http://192.168.1.6:3000/api/orders` - Бүх захиалга авах
- `POST http://192.168.1.6:3000/api/orders` - Шинэ захиалга үүсгэх

### Customers
- `GET http://192.168.1.6:3000/api/customers` - Бүх үйлчлүүлэгч авах

### OpenDataLab
- `GET http://192.168.1.6:3000/api/opendatalab/organization/:reg` - Байгууллагын мэдээлэл

## Чухал зүйлс

### ✅ Ажиллах нөхцөл:
1. ✅ Backend сервер ажиллаж байх ёстой (компьютер дээр)
2. ✅ Компьютер болон утас ижил WiFi сүлжээнд байх ёстой
3. ✅ Firewall нь port 3000-ийг блоклохгүй байх ёстой

### 🔍 Шалгах:
- Компьютер дээр: `http://localhost:3000` - Ажиллах ёстой
- Утас дээр: `http://192.168.1.6:3000` - Ажиллах ёстой

### ❌ Хэрэв ажиллахгүй бол:

1. **Backend сервер ажиллаж байгаа эсэхийг шалгах:**
   ```powershell
   netstat -ano | findstr :3000
   ```
   Хэрэв хоосон бол сервер ажиллахгүй байна. Ажиллуулах:
   ```powershell
   node server.js
   ```

2. **Firewall шалгах:**
   Windows Firewall нь port 3000-ийг блоклож байгаа эсэхийг шалгах
   
   Port нээх:
   ```powershell
   netsh advfirewall firewall add rule name="Node.js Server" dir=in action=allow protocol=TCP localport=3000
   ```

3. **IP хаяг шалгах:**
   Компьютерийн IP хаяг өөрчлөгдсөн эсэхийг шалгах:
   ```powershell
   ipconfig | findstr /i "IPv4"
   ```
   Хэрэв өөр IP байвал app settings дээр шинэчлэх

## Одоогийн тохиргоо

- **Backend URL**: `http://192.168.1.6:3000`
- **Компьютерийн IP**: `192.168.1.6`
- **Port**: `3000`
- **Status**: ✅ Сервер ажиллаж байна

## Тест хийх

### Browser дээр (утас):
1. Утасны browser нээх
2. `http://192.168.1.6:3000/api/products` оруулах
3. JSON мэдээлэл харагдах ёстой

### App дээр:
1. Settings → Server URL: `http://192.168.1.6:3000`
2. Connect дарах
3. "Connected" гэсэн мэдээлэл харагдах ёстой

Хэрэв бүх зүйл зөв тохируулагдсан бол утаснаас backend-д шууд хандах боломжтой! 🎉
