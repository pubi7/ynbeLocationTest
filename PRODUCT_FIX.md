# Барааны мэдээлэл дуудагдахгүй байгаа асуудлыг засах

## Асуудал
Барааны мэдээлэл app дээр харагдахгүй байна.

## Шалтгаан
Backend сервер нь pagination мэдээлэл буцаахгүй байсан, мөн барааны мэдээлэлд `stockQuantity` байгаагүй.

## Шийдэл

### 1. Backend сервер дахин ажиллуулах

Одоо ажиллаж байгаа серверийг зогсоож, дахин эхлүүлэх:

**Windows PowerShell дээр:**

```powershell
# Одоо ажиллаж байгаа Node process-уудыг зогсоох
Get-Process -Name node | Stop-Process -Force

# Сервер дахин ажиллуулах
cd c:\Users\purev\Downloads\ynbeLocationTest
node server.js
```

Эсвэл terminal дээр `Ctrl+C` дараад дахин `node server.js` ажиллуулах.

### 2. App дээр барааны мэдээлэл ачаалах

App дээр:

1. **Settings** руу орох
2. **Warehouse Connection** хэсэгт:
   - Server URL: `http://192.168.1.6:3000` байгаа эсэхийг шалгах
   - Хэрэв холбогдоогүй бол **Connect** дарах
   - Login: `admin@admin.com` / `password`
3. **Sync now** товч дарах (эсвэл app-ийг дахин ачаалах)

### 3. Шалгах

Барааны мэдээлэл харагдах ёстой:
- Sales Dashboard дээр "Зарагдах бараа" хэсэгт
- Sales Entry screen дээр бараа сонгох үед

## Засварласан зүйлс

### server.js файлд:

1. ✅ **Pagination нэмсэн** - `fetchAllProducts` method pagination шаарддаг тул
2. ✅ **stockQuantity нэмсэн** - Барааны мэдээлэлд stock мэдээлэл байх ёстой
3. ✅ **categoryName нэмсэн** - Category мэдээлэл зөв харуулахын тулд
4. ✅ **Илүү их mock бараа нэмсэн** - Тест хийхэд хялбар болгох

## Одоогийн Mock Бараанууд

1. Талх - 2500₮ (100 ширхэг)
2. Сүү - 3500₮ (50 ширхэг)
3. Өндөг - 500₮ (200 ширхэг)
4. Ундаа - 1500₮ (75 ширхэг)
5. Гурил - 4500₮ (30 ширхэг)
6. Бяслаг - 8000₮ (25 ширхэг)

## API Endpoint

```
GET http://192.168.1.6:3000/api/products?page=1&limit=200
```

Response формат:
```json
{
  "status": "success",
  "data": {
    "products": [...],
    "pagination": {
      "page": 1,
      "limit": 200,
      "total": 6,
      "totalPages": 1
    }
  }
}
```

## Хэрэв асуудал хэвээр байвал

1. **Backend сервер ажиллаж байгаа эсэхийг шалгах:**
   - Browser дээр: `http://192.168.1.6:3000/api/products`
   - JSON мэдээлэл харагдах ёстой

2. **App дээр connection шалгах:**
   - Settings → Warehouse Connection → Connected байгаа эсэх
   - Хэрэв холбогдоогүй бол Connect дарах

3. **Console log шалгах:**
   - Backend terminal дээр "Fetching products..." гэсэн мэдээлэл харагдах ёстой
   - App дээр error message байгаа эсэхийг шалгах

Хэрэв бүх зүйл зөв тохируулагдсан бол барааны мэдээлэл харагдах ёстой! 🎉
