# Мобайл дээр хийсэн захиалга Web дээр харагдах

## ✅ Тийм, мобайл дээр хийсэн захиалга web дээр харагдах ёстой!

## Хэрхэн ажилладаг

### 1. Мобайл дээр захиалга үүсгэх

Мобайл app дээр:
1. **Sales Entry** screen дээр бараа сонгох
2. Дэлгүүр сонгох
3. Төлбөрийн төрөл сонгох (Cash, Credit, Bank, Sales, Padan)
4. **Худалдан авалт хийх** товч дарах

### 2. Backend руу илгээх

App нь захиалгыг backend руу илгээдэг:
```
POST http://192.168.1.6:3000/api/orders
Body: {
  "customerId": 1,
  "items": [
    { "productId": 1, "quantity": 2 },
    { "productId": 2, "quantity": 1 }
  ],
  "orderType": "Store",
  "paymentMethod": "Cash"
}
```

### 3. Backend хадгална

Backend сервер захиалгыг `MOCK_ORDERS` array дээр хадгална:
- Order ID
- Order Number (ORD-{timestamp})
- Customer ID
- Items (бүтээгдэхүүн, тоо хэмжээ)
- Total Amount (тооцоолсон)
- Payment Method
- Status (pending)
- Created Date

### 4. Web дээр харагдах

Web interface нь захиалгуудыг авах:
```
GET http://192.168.1.6:3000/api/orders
```

## Шалгах арга

### 1. Мобайл дээр захиалга үүсгэх

1. App дээр Sales Entry screen руу орох
2. Дэлгүүр сонгох
3. Бараа нэмэх
4. Төлбөрийн төрөл сонгох
5. Худалдан авалт хийх

### 2. Backend console шалгах

Backend сервер console дээр дараах мэдээлэл харагдах ёстой:
```
Creating order: { customerId: 1, items: [...], paymentMethod: 'Cash' }
✅ Order created: ORD-1234567890, Total: 8500₮
```

### 3. API шалгах

Browser эсвэл Postman дээр:
```
GET http://192.168.1.6:3000/api/orders
```

Response:
```json
{
  "status": "success",
  "data": {
    "orders": [
      {
        "id": 1234567890,
        "orderNumber": "ORD-1234567890",
        "customerId": 1,
        "items": [
          { "productId": 1, "quantity": 2 },
          { "productId": 2, "quantity": 1 }
        ],
        "totalAmount": 8500,
        "paymentMethod": "Cash",
        "status": "pending",
        "createdAt": "2026-01-29T18:30:00.000Z"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 50,
      "total": 1,
      "totalPages": 1
    }
  }
}
```

### 4. Web interface дээр шалгах

Web dashboard дээр:
- Orders list дээр шинэ захиалга харагдах ёстой
- Order details дээр бүх мэдээлэл харагдах ёстой

## Засварласан зүйлс

### Backend сервер (`server.js`):

1. ✅ **Total Amount тооцоолох** - Items болон products-аас үнэ тооцоолно
2. ✅ **Order Number үүсгэх** - ORD-{timestamp} формат
3. ✅ **Pagination нэмсэн** - GET /api/orders endpoint pagination дэмжинэ
4. ✅ **Order details сайжруулсан** - Бүх мэдээлэл зөв хадгална

### Order Structure:

```javascript
{
  id: 1234567890,
  orderNumber: "ORD-1234567890",
  customerId: 1,
  items: [
    { productId: 1, quantity: 2 },
    { productId: 2, quantity: 1 }
  ],
  orderType: "Store",
  paymentMethod: "Cash",
  totalAmount: 8500,
  status: "pending",
  createdAt: "2026-01-29T18:30:00.000Z",
  createdById: 1,
  createdBy: { id: 1, name: "Admin User", ... }
}
```

## Одоо хийх зүйл

### 1. Backend сервер дахин ажиллуулах

```powershell
# Одоо ажиллаж байгаа Node process-уудыг зогсоох
Get-Process -Name node | Stop-Process -Force

# Сервер дахин ажиллуулах
cd c:\Users\purev\Downloads\ynbeLocationTest
node server.js
```

### 2. Мобайл дээр захиалга үүсгэх

1. App дээр Sales Entry screen руу орох
2. Дэлгүүр сонгох (жишээ: "Дэлгүүр 1")
3. Бараа нэмэх (жишээ: Талх - 2 ширхэг)
4. Төлбөрийн төрөл сонгох (Cash)
5. **Худалдан авалт хийх** товч дарах

### 3. Шалгах

**Backend console дээр:**
```
Creating order: { customerId: 1, items: [...], paymentMethod: 'Cash' }
✅ Order created: ORD-1234567890, Total: 5000₮
```

**Browser дээр:**
```
http://192.168.1.6:3000/api/orders
```
Захиалга харагдах ёстой.

## Чухал зүйлс

### ✅ Ажиллах нөхцөл:
1. ✅ Backend сервер ажиллаж байх ёстой
2. ✅ Мобайл app backend-тэй холбогдсон байх ёстой
3. ✅ Warehouse Connection → Connected байх ёстой

### 🔍 Шалгах:
- Backend console дээр "Creating order" мэдээлэл харагдах ёстой
- API endpoint дээр захиалга харагдах ёстой
- Web interface дээр захиалга list дээр харагдах ёстой

### ❌ Хэрэв ажиллахгүй бол:

1. **Backend сервер ажиллаж байгаа эсэхийг шалгах:**
   ```powershell
   netstat -ano | findstr :3000
   ```

2. **App дээр connection шалгах:**
   - Settings → Warehouse Connection → Connected байгаа эсэх
   - Хэрэв холбогдоогүй бол Connect хийх

3. **Console log шалгах:**
   - App console дээр: `📤 Warehouse backend руу захиалга илгээж байна...`
   - Backend console дээр: `Creating order: ...`

4. **API endpoint шалгах:**
   ```powershell
   curl http://192.168.1.6:3000/api/orders
   ```

## Одоогийн статус

- ✅ Мобайл app захиалга backend руу илгээдэг
- ✅ Backend захиалгыг хадгална
- ✅ Backend захиалгуудыг буцаадаг (GET /api/orders)
- ✅ Total amount зөв тооцоолно
- ✅ Order details бүрэн хадгална

Хэрэв бүх зүйл зөв тохируулагдсан бол мобайл дээр хийсэн захиалга web дээр харагдах ёстой! 🎉
