# 429 Error (Too Many Requests) Засах

## Асуудал
App дээр 429 алдаа гарч байна: "Too Many Requests" - хэт олон хүсэлт илгээсэн.

## Шалтгаан
App нь хэт олон хүсэлтийг хурдан дараалан илгээж байна:
- Login хүсэлт
- Profile хүсэлт  
- Products хүсэлт
- Shops хүсэлт

Эдгээр нь нэгэн зэрэг илгээгдэж байгаа нь rate limiting-д хүргэж байна.

## Засварласан зүйлс

### 1. Retry Logic засварласан
- ✅ 429 алдааг дахин оролдохгүй (retry хийхгүй)
- ✅ Зөвхөн timeout болон server errors (502, 503, 504) дахин оролдоно

### 2. Request Delays нэмсэн
- ✅ Connect хийхэд 300ms delay
- ✅ Profile fetch-ийн өмнө 200ms delay
- ✅ Products fetch-ийн өмнө 500ms delay
- ✅ Shops fetch-ийн өмнө 500ms delay

### 3. Error Handling сайжруулсан
- ✅ 429 алдааг тусгайлан боловсруулж, ойлгомжтой мэдээлэл харуулна
- ✅ "Хэт олон хүсэлт илгээсэн. Түр хүлээгээд дахин оролдоно уу."

### 4. Loading State засварласан
- ✅ API URL өөрчлөх үед loading state шалгана
- ✅ Олон хүсэлт нэгэн зэрэг илгээхээс сэргийлнэ

## Одоо хийх зүйл

### 1. App-ийг дахин build хийх
```powershell
flutter run
```

### 2. Settings дээр Connect хийх
1. Settings → Warehouse Connection
2. Server URL: `http://192.168.1.6:3000` (trailing slash байхгүй байх ёстой)
3. Email: `admin@admin.com`
4. Password: `password`
5. **Connect & Sync** товч дарах
6. Түр хүлээх (хүсэлтүүд дараалан илгээгдэнэ)

### 3. Хэрэв 429 алдаа гарвал
- 10-30 секунд хүлээгээд дахин оролдох
- "Connect & Sync" товчийг олон удаа дарахаас зайлсхийх
- Backend сервер ажиллаж байгаа эсэхийг шалгах

## Алдаа засах

### Хэрэв 429 алдаа хэвээр гарвал:

1. **Backend сервер шалгах:**
   ```powershell
   # Backend ажиллаж байгаа эсэхийг шалгах
   curl http://192.168.1.6:3000
   ```

2. **App-ийг бүрэн restart хийх:**
   - App-ийг хаах
   - Дахин нээх
   - Settings → Disconnect дарах
   - Дахин Connect хийх

3. **Server URL шалгах:**
   - Trailing slash байхгүй байх ёстой: `http://192.168.1.6:3000` ✅
   - Trailing slash байвал: `http://192.168.1.6:3000/` ❌

4. **Backend сервер дахин ажиллуулах:**
   ```powershell
   # Node process-уудыг зогсоох
   Get-Process -Name node | Stop-Process -Force
   
   # Дахин ажиллуулах
   cd c:\Users\purev\Downloads\ynbeLocationTest
   node server.js
   ```

## Техникийн мэдээлэл

### Засварласан файлууд:
- `lib/services/warehouse_web_bridge.dart` - Retry logic засварласан
- `lib/providers/warehouse_provider.dart` - Delays нэмсэн, error handling сайжруулсан
- `lib/screens/settings/settings_screen.dart` - Connect flow-д delays нэмсэн

### Request Flow (одоо):
1. Connect (300ms delay)
2. Login request
3. Profile request (200ms delay)
4. Products request (500ms delay)
5. Shops request (500ms delay)

Нийт: ~1.5 секунд завсартай дараалан илгээгдэнэ.

Хэрэв бүх зүйл зөв тохируулагдсан бол 429 алдаа гарч болохгүй! 🎉
